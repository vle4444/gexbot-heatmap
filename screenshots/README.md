# Screenshots

Add `delta.png` (and any others you want to reference from the README) here.

Suggested captures:
- `delta.png` — main delta.html view with heatmap + profile panel
- `classic.png` — index.html classic heatmap
- `help.png` — the help overlay open

The README currently references `delta.png`. Either add one or remove the
image tag from `README.md`.
